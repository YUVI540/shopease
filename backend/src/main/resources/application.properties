# =============================================
# ShopEase Application Configuration
# =============================================

# Server
server.port=8080


# # # MySQL Database — update password before running!
# # spring.datasource.url=jdbc:mysql://localhost:3306/shopease?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
# # spring.datasource.username=root
# # spring.datasource.password=yuvi1234
# # spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver


# # JPA / Hibernate
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.format_sql=true
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# # App name
spring.application.name=shopease-backend

# ✅ Railway MySQL (DIRECT CONNECTION)
spring.datasource.url=jdbc:mysql://shuttle.proxy.rlwy.net:12644/railway?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=RqZnPnBTzXnpldTFOWjTTdKeytPfWhVT

# ✅ Driver
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ✅ Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# ✅ IMPORTANT FIX (Dialect error)
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# ✅ Server port (Render requirement)
server.port=10000
